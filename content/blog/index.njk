---
layout: layouts/page
permalink: /blog/
eyebrow: arquivo
title: Blog
description: Todos os posts publicados até agora, em ordem cronológica.
eleventyComputed:
  meta: >
    {{ collections.posts.length }} post{% if collections.posts.length != 1 %}s{% endif %} publicado{% if collections.posts.length != 1 %}s{% endif %}.
eleventyNavigation:
  key: Blog
  order: 3
---

{% if collections.posts.length > 0 %}
    <ol class="timeline" role="list">
        {% for post in collections.posts %}
            {% set visibleTags = post.data.tags | filterTagList %}
            <li class="timeline__item">
                <article class="timeline__entry">
                    <time class="timeline__date" datetime="{{ post.date | htmlDateString }}">
                        {{ post.date | readableDate("dd 'de' LLL yyyy") }}
                    </time>

                    <h2 class="timeline__title">
                        <a href="{{ post.url }}#main">{{ post.data.title }}</a>
                    </h2>

                    {% if post.data.description %}
                        <p class="timeline__excerpt">{{ post.data.description }}</p>
                    {% endif %}

                    {% if visibleTags and visibleTags.length %}
                        <ul class="tag-list tag-list--muted timeline__tags" role="list" aria-label="Tags do post">
                            {% for tag in visibleTags %}
                                <li class="tag-list__item">{{ tag }}</li>
                            {% endfor %}
                        </ul>
                    {% endif %}
                </article>
            </li>
        {% endfor %}
    </ol>
{% else %}
    <p>Nenhum post encontrado.</p>
{% endif %}
